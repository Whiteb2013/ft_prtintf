{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_printf",
            "type": "shell",
            "command": "make re && gcc -g -I ./srcs -I ./libft main_test_double.c -L. -lftprintf -o printf && make fclean",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
